# AI Agent 岗位模拟面试记录

## 用户诉求

用户希望基于个人简历与目标岗位 JD 进行一场真实风格的模拟面试，由 Codex 担任面试官，根据回答进行连续追问，并评估候选人与岗位的匹配度。

## 候选人背景

- 3 年通信研发经验，现就职于华为技术有限公司。
- 求职方向：算法工程师。
- 主要 AI 项目：
  - 面向无线通信领域的 CodeAgent。
  - 基于 GraphRAG 的 OCL 检索增强与代码生成系统。
- CodeAgent 项目涉及：
  - `prd-assist -> prd-plan -> prd-apply` 三阶段研发流水线。
  - Multi-Agent 编排、任务依赖与断点恢复。
  - 分层上下文管理、按需检索、工具结果预算与压缩。
  - 基于 Commit Diff、编译测试和执行 Trace 的 Skill 自演进思路。
- GraphRAG 项目涉及：
  - Neo4j 图索引与 Milvus 向量索引。
  - 向量召回、图关系扩展与结果融合。
  - LLM 查询路由、多跳查询及结构化代码生成。

## 目标岗位 JD

### 岗位职责

1. 主导量化私募基金公司对外业务、对内管理的 AI Agent 系统工程化落地，覆盖金融业务以及财务、法务、人事等内部管理场景，建设可用、可靠、可观测、可维护的智能体服务。
2. 作为公司 AI 技术领域的领军者，高强度使用顶级模型，体现极客精神并推动全员 AI 应用。

### 岗位期望

1. 真正理解并搭建过 Agent Harness/Runtime，包括上下文管理、工具调用、子 Agent 调度、记忆处理、工作流编排、权限及合规管控、成本控制、评测回归、协作维护和版本迭代。
2. 能理解有价值的业务与管理场景，将其抽象为技术语言，形成可工程化落地的 Agent 系统。
3. 能高效使用 Vibe Coding，同时能够亲自用 Python 编写可靠的工程代码。
4. 熟悉 LangGraph、RAG 等技术，但不局限于既有框架，对快速发展的 AI 技术保持好奇心与敬畏。
5. 本科以上学历，具有 1 年以上 LLM 或 Agent 深入实践经验。

## 初始匹配分析

### 明显匹配项

- 候选人的 CodeAgent 项目覆盖了工作流、上下文、工具反馈、Multi-Agent、执行验证与持续优化等 Runtime 核心模块。
- 候选人有大型工程项目交付与跨团队协作经验，适合岗位对“主导工程化落地”的要求。
- 候选人具备通信领域复杂业务抽象经验，与金融和内部管理场景所需的领域建模能力具有可迁移性。
- 候选人同时具备 Python、RAG、图检索和 Agent 工程经验。

### 面试中的核心验证点

- CodeAgent 是候选人亲自实现的生产级 Runtime，还是方案设计或基于现成框架的流程封装。
- 权限、合规、审计、工具隔离和敏感数据处理是否有真实实践。
- 评测数据的样本定义、基线、统计口径和可复现性。
- Agent 失败时的状态管理、幂等性、重试、补偿与人工接管机制。
- 能否把通信领域经验迁移到量化私募的金融、财务、法务和人事场景。
- Python 编码与系统设计能力是否达到可独立建设核心基础设施的水平。
- 是否具备主动探索模型能力边界、推动非技术人员使用 AI 的产品意识和极客特质。

## 面试过程

### 模拟规则

- 每轮由面试官给出问题，同时提供一份参考回答。
- 参考回答只依据候选人简历与候选人后续确认的信息组织，不将尚未确认的设计或实现表述为真实经历。
- 用户实际回答后，继续记录：
  - 用户原始回答。
  - 面试官的针对性追问。
  - 回答中的亮点、风险与证据缺口。
  - 更适合真实面试表达的改进版回答。
- 对话过程持续追加到本文档，保留完整案例上下文。

### 第一轮：自我介绍与岗位匹配

#### 面试官问题

你好，鞠巍。看完你的简历，我注意到你此前主要从事无线通信研发，而我们希望招聘一位能够主导公司 AI Agent 系统落地的人。

请你用 3 分钟介绍自己，并重点回答三个问题：

1. 为什么你认为自己不是“做过两个 AI 项目的通信工程师”，而是能够承担 Agent 核心系统建设的人？
2. 你提到的 CodeAgent 中，哪些核心模块是你亲自设计和实现的？
3. 如果让你加入后首先选择一个金融或内部管理场景落地，你会选什么？为什么？

#### 参考回答

面试官您好，我叫鞠巍，硕士毕业于上海大学信息与信号处理专业，目前在华为从事通信软件研发，已有 3 年工作经验。我的经历可以分成两个阶段：前期主要负责 LTE 物理层和基站单板软件研发，后期重点参与大模型在无线通信研发场景中的工程化落地。

我认为自己并不只是“做过两个 AI 项目的通信工程师”，主要有三个原因。

第一，我对 Agent 的理解不是停留在调用模型或搭建一个问答应用上。在无线通信 CodeAgent 项目中，我们面对的是大型存量代码库、复杂接口依赖、长验证链路以及高可靠性交付要求。我参与建设了从需求材料补全、代码影响分析、任务拆解，到代码修改、编译测试和错误反馈修正的闭环流程。这个过程中真正困难的部分，是如何管理上下文、工具调用、任务依赖、执行状态和验证反馈，这些问题与贵公司希望建设的 Agent Harness 或 Runtime 高度一致。

第二，我具备把复杂领域知识转化为工程系统的经验。无线通信和金融虽然业务知识不同，但工程方法具有共通性：首先识别稳定规则、动态任务状态和敏感数据边界，再将业务流程拆成可执行步骤，为每一步定义输入、输出、权限、校验条件和失败处理机制。我过去既做过通信需求和跨团队版本交付，也做过 CodeAgent 与 GraphRAG，因此能够同时理解业务约束和技术实现。

第三，我重视 Agent 的最终交付结果，而不只关注模型输出效果。简历中的试点任务会通过编译、测试和人工修订结果进行验证。例如，CodeAgent 首轮编译率约为 40%，经过编译反馈驱动的 2 到 3 轮自动修正后，最终通过率约为 90%；我们还通过 AI Commit 与人工修订 Commit 的差分，分析生成代码的有效保留率。这种基于外部证据评估 Agent 的方式，也可以迁移到金融业务中的数据核验、流程完成率、人工接管率和合规检查。

在 CodeAgent 中，根据我目前简历能够确认的职责，我重点参与了三阶段研发流水线、Multi-Agent 协作机制、分层上下文体系，以及基于 Commit Diff、编译测试和执行 Trace 的持续改进闭环设计。真实面试时，我会进一步明确每个模块中由我亲自完成的代码、接口和关键技术决策，并区分个人贡献与团队成果，避免只描述整体方案。

如果加入贵公司，我不会一开始就选择全自动投资决策这类高风险场景。我会优先选择“内部制度与业务材料检索，加上受控流程执行”作为首个落地点，例如合同审查辅助或财务报销审核助手。以合同审查为例，Agent 可以检索公司制度、历史合同模板和法务规则，识别缺失条款与风险点，生成带引用依据的审查意见，但最终审批仍由法务人员完成。这个场景业务价值清晰，也便于建立权限隔离、引用溯源、人工确认、审计日志和离线评测，可以先验证 Runtime 的通用能力，再扩展到人事、财务和对外业务。

我希望应聘这个岗位，是因为它既要求深入建设 Agent 底层工程能力，也要求把能力真正嵌入业务。我过去在复杂通信软件、跨团队交付和 Agent 工程上的经历，使我能够从一个受控但有实际价值的场景开始，逐步建设可复用、可评测、可审计的 Agent 平台。

#### 证据边界与作答提醒

- “亲自设计和实现”的具体模块、代码量、技术栈与所有权目前尚未由候选人确认。正式作答时必须根据实际情况修改。
- 合同审查或报销审核是面向该 JD 给出的场景建议，不属于候选人既有项目经历。
- 简历中的成功率、准确率、召回率和响应时间等指标，需要候选人能够解释数据集、样本量、基线、计算方式与复现流程。

#### 候选人原始回答

> 面试官您好，我叫鞠巍，毕业于上海大学通信学院，目前在华为从事LTE通信相关的研发工作。我的工作经历大体可以分为两部分，一部分是从事LTE无线通信相关的开发工作，一部分是agent在无线通信领域的工程化落地。
>
> 第一个问题：
>
> 1、我对Agent的认知并不是局限于模型搭建和简单地问答循环。在无线通信codeAgent项目中，面对庞大且极其复杂的LTE存量业务代码，我搭建了需求补全、代码影响分析、任务拆解、代码生成、编译测试、错误修正的完整闭环。项目涉及上下文管理、工具调度、任务依赖等环节，和agent harness底层建设强相关
>
> 2、我具备把复杂领域知识抽象成可复用skill的能力，虽然通信与金融领域业务不同，但工程方法论必然是类似的：梳理稳定规则、数据边界、把业务流程拆解成标准化的执行步骤等。同时，我担任组内问题接口人，具备丰富的问题定位经验，也交付过许多涉及多领域的复杂需求，这些项目交付、问题闭环的能力是可迁移的
>
> 第二个问题：
>
> 在 CodeAgent 项目里，我主导设计并实现三阶段研发流水线、多智能体协作机制、分层上下文体系，以及依托编译日志、执行 Trace、代码 Diff 构建的持续优化闭环。
>
> 第三个问题：
>
> 如果入职落地首个场景，我会优先落地内部文档检索 + 受控流程辅助执行场景。Agent 负责资料检索、风险识别、输出带溯源依据的参考意见，关键决策交由人工把控。风险可控、价值直观，方便落地审计、权限、评测体系，验证平台基础能力后再拓展更多业务。

#### 面试官评价

总体评价：岗位方向匹配，结构清楚，能够主动把通信经验映射到 Agent Runtime 和企业业务落地，但技术证据不足，暂时只能证明“理解这些概念并参与过项目”，还不能充分证明“能够独立建设核心系统”。

##### 回答亮点

1. 没有把 Agent 简化成模型调用或问答应用，主动提到了影响分析、任务拆解、代码修改、编译测试和错误修正闭环。
2. 强调了复杂领域知识抽象、标准流程和数据边界，能够与目标岗位的业务工程化要求形成对应关系。
3. 首个场景选择较稳健，体现了风险分级意识，没有直接选择自动交易或其他高风险决策场景。
4. 能够主动提到权限、审计和评测，方向上符合金融公司的治理要求。

##### 主要问题

1. “我搭建了”“我主导设计并实现”是很强的所有权表述，但没有说明具体写了哪些模块、使用什么技术、关键接口是什么、团队如何分工。
2. 罗列了上下文管理、工具调度、多 Agent、Trace、Diff 等概念，但没有解释其中最困难的工程问题以及最终采用的机制，容易被判断为概念堆叠。
3. “工程方法论必然类似”的表述过于绝对。通信软件与金融系统在数据时效性、监管责任、权限模型和错误成本上存在明显差异，更好的表达是“部分工程方法可以迁移，但需要重新识别金融领域特有的风险边界”。
4. 首个业务场景仍然比较抽象。需要明确目标用户、现有流程、输入输出、可调用工具、人工审批点和价值指标。
5. 缺少量化结果。可以用简历中的 12 个试点任务、首轮编译率、最终通过率和人工保留率支撑，但必须能解释统计口径。

#### 更适合面试的表达调整

- 将“工程方法论必然是类似的”改为：“通信与金融的领域规则和风险等级不同，但任务拆解、状态管理、权限隔离、过程审计和结果验证等工程方法具有可迁移性；入职后仍需与业务、合规人员共同重新界定金融场景的边界。”
- 在“主导并实现”之后补充一个能够接受代码级追问的实例，例如：“我负责定义任务状态模型、阶段产物协议和失败恢复逻辑，并用 Python 实现编排与工具适配层。”只有在符合真实经历时才可采用。
- 首个场景应给出具体例子，例如合同审查辅助，并明确“检索制度和合同模板 -> 识别风险条款 -> 输出带引用建议 -> 法务确认 -> 记录审计日志”的执行链路。

### 第二轮：CodeAgent Runtime 深挖

#### 面试官追问

你刚才说自己“主导设计并实现”了三阶段流水线、多智能体协作、分层上下文和持续优化闭环。请不要再概括整个项目，选择其中一个你亲自编码最多、最能体现 Agent Runtime 能力的模块，回答以下问题：

1. 这个模块的输入、输出和核心数据结构是什么？
2. Agent 的状态如何保存？一次任务执行失败后如何恢复，怎样避免重复执行工具造成副作用？
3. 你当时遇到的最困难问题是什么？比较过哪些方案，为什么最终选择当前方案？
4. 请明确区分哪些代码和技术决策由你完成，哪些是团队其他成员完成的。

#### 参考回答

我选择介绍三阶段流水线中的任务编排与状态恢复模块，因为这是我亲自参与设计和编码最多，也最能体现 Runtime 能力的部分。

它的输入不是一段简单 Prompt，而是一个结构化任务对象，主要包含任务 ID、代码仓版本、需求材料引用、当前阶段、阶段依赖、允许调用的工具、预算限制和验收条件。三个阶段分别生成版本化的 `DESIGN.md`、`TASK.md` 以及代码变更和验证报告。每个任务项还会记录目标文件、依赖任务、执行状态、重试次数、工具调用记录和验证结果。

在状态管理上，我把任务状态分为待执行、执行中、待验证、已完成、可重试失败、不可恢复失败和等待人工处理。状态转换由明确的事件触发，例如设计检查通过、代码 Patch 应用成功、编译完成或测试失败。每完成一个阶段，就持久化阶段产物、状态、代码基线和关键工具结果。任务恢复时不依赖模型回忆历史对话，而是读取最近一次已提交的检查点，重新装配当前阶段所需上下文。

为避免恢复后重复调用工具产生副作用，我会区分只读工具和有副作用工具。代码检索、文件读取等只读操作允许安全重试；代码修改、提交或外部写入操作需要携带任务 ID、步骤 ID 和尝试编号，并在执行前检查对应操作是否已经成功记录。代码修改优先生成 Patch，再由受控执行器检查基线和目标文件版本后应用。对于编译和测试这类可重复但成本较高的操作，会缓存输入代码版本、命令和环境指纹，在输入没有变化时复用结果。

最困难的问题是模型生成的计划与真实代码依赖经常不一致。如果直接让 Agent 从需求一步生成代码，失败后很难判断是需求理解错误、影响域遗漏还是实现错误。我们比较过单 Agent 长上下文闭环和阶段化流水线。单 Agent 方案实现简单，但上下文不断膨胀，失败定位和断点恢复都比较困难；最终采用阶段化方案，把设计、任务拆解和代码执行分别固化为可校验产物，并在阶段边界设置检查条件。这样增加了流程开销，但获得了更强的可观测性和恢复能力。

在个人分工方面，我负责的部分是：任务与阶段状态结构设计、阶段产物协议、上下文装配规则、执行状态持久化以及失败后恢复流程，并参与 Python 编排层和编译反馈适配器的实现。代码检索底座和部分领域工具由其他同事负责，编译环境和测试用例由原有研发平台提供。这里需要根据候选人的真实分工调整；如果并未实现持久化、幂等键、工具缓存或上述状态结构，则不应在正式面试中这样回答。

#### 上下文与 Memory 机制方向的参考回答

我选择介绍 Session Memory 与上下文压缩模块。这里的 Memory 不是知识库，而是长任务中的结构化工作记录，用于在历史消息过长时保留当前状态和关键决策。

模块输入是当前会话的 `Message[]`。我用消息 UUID 和 `lastSummarizedMessageId` 记录 Memory 已经覆盖到哪条消息。Memory 按固定模板保存当前状态、任务目标、重要文件、错误修正和下一步动作。上下文增长到一定程度后，post-sampling hook 会启动隔离的子 Agent 更新这份文件；子 Agent 只允许编辑指定的 Memory 文件，避免影响主 Agent 的工具状态。

真正发生压缩时，模块输出一个 `CompactionResult`，主要包含压缩边界、Memory 摘要、需要原样保留的近期消息以及 Plan、Hook 等附件。新的上下文由“压缩边界 + 摘要 + 近期消息 + 附件”重新组成，而不是重新发送全部历史。

这里最难的问题是压缩后仍要保证工具调用链合法。不能简单按消息下标截断，否则可能保留了 `tool_result`，却丢掉对应的 `tool_use`。我的处理方式是确定截断点后再向前检查并补齐工具调用对和同一 Assistant 消息的 thinking block。如果已总结消息的 UUID 找不到，说明会话历史发生了变化，就回退到传统压缩流程，而不是继续使用不可靠的边界。

上下文恢复只重建消息，不会重新执行历史工具，因此不会因为 compact 本身重复修改代码。但业务工具是否实现幂等，仍然属于执行层的职责，不能说 Memory 模块已经解决了所有副作用问题。

方案上，我们比较过完整保留历史、到阈值后临时生成一次摘要，以及持续维护结构化 Memory 三种方式。最终采用第三种，因为它能在自然停顿点持续更新状态，压缩时只需替换已总结历史并保留消息尾部，连续性更好；代价是需要处理抽取并发、边界丢失和摘要错误，所以保留了传统 compact 作为兜底。

个人分工方面，如果与真实经历一致，我会回答：“我负责 Memory 更新触发、模板和 token 预算、总结边界维护、压缩结果组装，以及工具调用对的边界修正和失败回退；主消息循环和模型 API 接入由其他同事负责。”

#### 完整机制展开（备查，不建议连续口述）

以下内容用于准备面试官的进一步追问。实际面试中应先使用上面的精简回答，再根据追问选择相关部分展开。

##### 1. 模块定位与数据结构

Session Memory 不是通用知识库或向量检索，而是当前长任务的结构化工作记录；上下文压缩也不是简单删除旧消息，而是用这份持续更新的记录替换已经总结过的历史，同时保留连续执行所需的近期消息。

模块输入主要是当前会话的 `Message[]`、模型上下文窗口和自动压缩阈值。每条消息带有 UUID，Assistant 消息可能包含 `tool_use`，User 消息包含与之配对的 `tool_result`。模块用 `lastSummarizedMessageId` 标记 Session Memory 已经覆盖到哪条消息，并配置首次启用所需的上下文 token、两次更新之间的最小 token 增量和工具调用数量。

Memory 是一份结构固定的 Markdown，主要包含：

- `Current State`：当前状态
- `Task specification`：任务说明
- `Files and Functions`：重要文件与函数
- `Workflow`：执行流程
- `Errors & Corrections`：错误与修正
- `Codebase and System Documentation`：代码库与系统说明
- `Learnings`：经验与认知
- `Key results`：关键结果
- `Worklog`：工作记录

固定结构使任务目标、当前状态、重要文件、失败经验和下一步动作各自有稳定位置。每个 section 有长度限制，整份 Memory 也有总 token 预算；超限时压缩低价值的旧记录，并优先保持 `Current State` 和 `Errors & Corrections` 准确。

压缩的核心输出是：

```text
CompactionResult {
  boundaryMarker,
  summaryMessages,
  messagesToKeep,
  attachments,
  hookResults,
  preCompactTokenCount,
  postCompactTokenCount,
  truePostCompactTokenCount
}
```

`boundaryMarker` 标记新旧上下文分界；`summaryMessages` 是由 Session Memory 构造、带有 `isCompactSummary` 标记的摘要；`messagesToKeep` 是需要原样保留的近期消息；`attachments` 用于恢复 Plan 等附加上下文；`hookResults` 用于重新注入 compact 阶段的 SessionStart Hook 结果。重建顺序为：

```text
boundaryMarker
→ summaryMessages
→ messagesToKeep
→ attachments
→ hookResults
```

##### 2. Memory 更新与隔离

Memory 更新不在主 Agent 循环中同步完成。每轮采样结束后，post-sampling hook 判断是否需要抽取：

1. 首次启用必须达到初始化 token 阈值。
2. 后续更新必须达到最小上下文增长量。
3. 在满足 token 条件的前提下，工具调用数量达到阈值，或者当前处于没有工具调用的自然停顿点。

满足条件后，Runtime 启动隔离的 forked agent，读取已有 Memory，并且只允许它使用 Edit 工具修改指定的 Memory 文件，禁止调用其他工具。这可以避免笔记抽取修改主 Agent 的工具状态，同时利用主会话的缓存上下文降低后台整理成本。

##### 3. 压缩、恢复与 API 约束

正常压缩时，通过 `lastSummarizedMessageId` 找到已经总结的消息边界，再选择边界之后的消息作为近期上下文。但截断点不能只按数组下标确定，否则可能把 `tool_use` 与对应的 `tool_result` 拆开，产生孤立的工具结果，导致 Messages API 拒绝请求。

因此，计算出初始保留位置后还要向前检查：

- 如果保留范围内存在 `tool_result`，必须补回对应的 `tool_use`。
- 如果流式产生的多条 Assistant 消息共享同一个 message ID，还要补回同组的 thinking block，使后续消息归一化能够正确合并。
- 旧的 `compact_boundary` 要从 `messagesToKeep` 中过滤，避免恢复时再次触发错误裁剪。

如果 `lastSummarizedMessageId` 在当前消息列表中找不到，说明历史可能已经变化，无法可靠确定已总结边界。此时不猜测边界，而是放弃 Session Memory 快速压缩，回退到传统 compact 流程。

恢复会话时还可能出现 Memory 文件存在、但进程内 `lastSummarizedMessageId` 已丢失的情况。此时仍可用 Session Memory 构造摘要，但要按恢复会话分支重新计算保留窗口。压缩前还会等待正在执行的 Memory 抽取；等待有超时，抽取状态长时间未结束则视为陈旧，防止压缩流程永久阻塞。

##### 4. 副作用与幂等边界

从这套实现中能够确认的副作用控制有两点：

1. Memory 抽取 Agent 只能编辑唯一指定的 Memory 文件，不能执行 Bash、提交代码或调用其他业务工具。
2. 压缩只是重建已有消息，不会重新播放历史 `tool_use`，因此 compact 本身不会再次执行代码修改。

但这不等于系统已经为所有业务工具提供 exactly-once 语义。如果恢复时无法确认某项外部写入是否成功，仍需要工具执行层通过幂等键、结果持久化或执行前读取真实环境来核验。这是 Memory 与 compact 模块的明确能力边界。

##### 5. 方案比较

可以比较三种方案：

1. **完整保留历史**：实现简单、信息完整，但上下文和成本持续增长。
2. **到阈值后临时生成一次摘要**：流程较简单，但只在压缩发生时整理历史，容易遗漏当前状态，压缩延迟也更集中。
3. **持续维护结构化 Session Memory**：在自然停顿点增量更新，压缩时用 Memory 替换已总结历史，并保留近期消息尾部。

第三种方案能够用 `lastSummarizedMessageId` 明确划分已总结和未总结内容，压缩时延也更低，因此更适合长任务。代价是需要处理 Memory 更新并发、进程恢复后的边界丢失、section 超限和摘要错误累积，所以仍需保留传统 compact 作为回退路径。

##### 6. 验证方式

可以重点验证以下场景：

- token 或工具调用条件不足时，不应频繁启动 Memory 抽取。
- 截断点附近存在 `tool_use/tool_result` 时，重建后的消息仍满足 API 约束。
- 多条流式 Assistant 消息共享 message ID 时，thinking 和工具调用内容不会被错误拆分。
- `lastSummarizedMessageId` 不存在、Memory 为空或抽取超时时，可以安全回退。
- Memory 超长时，注入上下文的内容受到预算限制，同时保留完整 Memory 文件供后续读取。

验收指标可以包括压缩前后 token 数、压缩后任务继续成功率、关键约束保留率、工具调用配对完整率和传统压缩回退成功率。

##### 7. 个人贡献边界

如果与真实项目经历一致，可以表述为：

> 我负责 Memory 抽取触发器、固定模板和预算控制、`lastSummarizedMessageId` 边界维护、`CompactionResult` 组装，以及工具调用对的边界修正和失败回退；主消息循环、模型 API 客户端和文件编辑工具由团队其他成员负责。

如果实际工作只是研究或借鉴该实现，则应改为“我分析了这套机制，并据此设计或改进了自己的上下文方案”，不能表述成自己完成了 Claude Code 的相关代码。

#### 机制依据与证据边界

- 上述具体实现链路参考自 `E:\ClaudeCode\claude-code-main` 的 Claude Code 社区逆向复现代码，主要代码证据为 `src/services/SessionMemory/sessionMemory.ts`、`sessionMemoryUtils.ts`、`prompts.ts`、`src/services/compact/sessionMemoryCompact.ts`、`compact.ts`、`autoCompact.ts` 和 `src/query.ts`。该仓库不是 Anthropic 官方源码，因此这些字段、阈值和分支只能表述为“社区复现代码中的实现”，不能表述为 Anthropic 官方实现事实。
- Anthropic 官方公开文章确认了更高层的设计原则：长任务需要通过 compaction 和 structured note-taking 管理有限上下文；文章还明确说明 Claude Code 会压缩消息历史并保留关键决策、未解决问题和近期文件，但没有公开上述复现代码中的完整类型、阈值和内部调用链：[Effective context engineering for AI agents](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)。
- 本参考回答展示的是如何把社区复现代码中的具体机制组织成面试答案，不证明候选人的 CodeAgent 已采用同样实现。正式回答必须把模块名称、语言、配置值、个人代码边界和验证结果替换成候选人的真实经历。

#### 第二轮验证重点

- 是否能够从概念层下沉到数据结构、状态转换、失败模型和接口设计。
- 是否真正理解断点恢复与幂等性，而不只是重新发送历史消息。
- 是否能诚实、清楚地区分个人贡献与团队成果。
- 参考回答中的结构和机制是面试表达范例，不代表候选人项目已经采用；候选人必须按真实实现作答。
