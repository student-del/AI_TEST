# Agent 循环机制详解

> 本文是对 Claude Code 架构学习指南中第 3 节的深入展开，详细剖析 Agent 循环的每一步设计和背后的工程决策。

---

## 一、本质：一个极简的 ReAct 循环

Claude Code 的核心循环简单到令人难以置信：

```python
while claude_response.has_tool_call:
    result = execute_tool(tool_call)
    claude_response = send_to_claude(result)
return claude_response.text
```

**这就是全部**。所有接口——CLI、Headless（`-p`）、SDK、IDE 扩展——最终都汇聚到同一个 `queryLoop` 函数上。**不允许多个执行引擎并存**。这个架构决策避免了不同入口点产生不同行为的问题。

约 **98.4%** 的代码是围绕这个简单循环的决定性基础设施（如上下文管理、权限、沙箱、恢复机制等），只有约 1.6% 是 AI 决策逻辑本身。循环本身很简单，工程工作在于围绕它的各个子系统。

---

## 二、完整 9 步管道

每个对话回合按以下固定管线顺序执行：

```
用户输入
  │
  ▼
┌──────────────────────────────────────────────┐
│ 第 1 步：设置解析 (Settings Resolution)       │
│ 加载 settings.json、.claude.json、           │
│ settings.local.json、项目级配置等             │
└──────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────┐
│ 第 2 步：状态初始化 (State Initialization)    │
│ 恢复或创建会话转录（JSONL）、加载记忆索引、   │
│ 初始化工具注册表                             │
└──────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────┐
│ 第 3 步：上下文组装 (Context Assembly)        │
│ 从 9 个来源按固定优先级组装完整的上下文，      │
│ 这是模型最终"看到"的所有内容                 │
└──────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────┐
│ 第 4 步：预模型整形 (Pre-Model Shapers)       │
│ 5 个阶段的压缩管线，按成本从低到高依次执行，  │
│ 确保上下文不超过窗口限制且保持缓存友好        │
└──────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────┐
│ 第 5 步：模型调用 (Model Call)                │
│ 将组装并整形好的上下文发送给 LLM API，        │
│ 流式接收文本和 tool_use 块                    │
└──────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────┐
│ 第 6 步：工具派发 (Tool Dispatch)             │
│ 解析 LLM 响应中的 tool_use 块，               │
│ 转换为内部工具调用指令                        │
└──────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────┐
│ 第 7 步：权限门控 (Permission Gate)           │
│ 4 级授权管线依次检查：                        │
│ 预过滤 → Hook → 规则评估 → 权限处理           │
└──────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────┐
│ 第 8 步：工具执行 (Tool Execution)            │
│ 执行通过权限检查的工具调用，收集执行结果      │
│ 和可能的错误信息                              │
└──────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────┐
│ 第 9 步：停止条件检查 (Stop Condition Check)  │
│ · 无更多 tool_use → 停止，返回最终文本        │
│ · 达到最大轮次上限 → 停止                     │
│ · 用户中断 → 停止                             │
│ · 否则 → 带着工具结果回到第 3 步              │
└──────────────────────────────────────────────┘
```

---

## 三、第 3 步深入：9 个上下文来源的组装

上下文组装不是简单的字符串拼接，而是有严格优先级和分层结构的：

### 3.1 组装结构（按最终顺序）

```
┌──────────────────────────────────────────────┐
│ 1. 系统提示（System Prompt）                  │
│    角色定义、行为规范、能力边界               │
│    永远在最前面，不可被任何来源覆盖           │
├──────────────────────────────────────────────┤
│ 2. 环境信息                                  │
│    · Platform: win32 / linux / darwin        │
│    · Shell: bash / zsh / powershell          │
│    · Workspace Folder: E:\AI_TEST            │
│    · Current Date: 2026-05-29                │
│    · 模型信息：claude-sonnet-4-6 等           │
├──────────────────────────────────────────────┤
│ 3. CLAUDE.md 层级（4 级合并）                 │
│    系统级 → 用户级 → 项目级 → 本地级          │
│    注意：这是"用户上下文"（概率性遵守）       │
│    不是系统提示（确定性执行）                  │
├──────────────────────────────────────────────┤
│ 4. 路径作用域规则                            │
│    定义哪些文件/目录是模型可以访问的          │
│    防止意外或恶意访问敏感路径                 │
├──────────────────────────────────────────────┤
│ 5. 自动记忆                                  │
│    从 MEMORY.md 索引中选出最多 5 条           │
│    与当前对话最相关的记忆                     │
│    每条记忆包含 frontmatter + 正文            │
├──────────────────────────────────────────────┤
│ 6. 工具元数据                                │
│    所有可用工具的名称、描述、参数 schema       │
│    动态调整：被 deny 的工具不出现在此         │
│    MCP 工具的可用性也会实时反映               │
├──────────────────────────────────────────────┤
│ 7. 对话历史                                  │
│    当前会话的完整历史（或压缩后的版本）        │
│    User → Assistant → Tool Use → Tool Result  │
│    按时间顺序排列                             │
├──────────────────────────────────────────────┤
│ 8. 工具结果（最新）                           │
│    最新一轮工具调用的返回值                   │
│    可能是文件内容、搜索结果、命令输出等        │
├──────────────────────────────────────────────┤
│ 9. 压缩摘要                                  │
│    Auto-Compact 生成的早期对话摘要            │
│    只在 5 阶段压缩管线触发了 Auto-Compact     │
│    时才会出现                                 │
└──────────────────────────────────────────────┘
```

### 3.2 核心设计决策

**CLAUDE.md 是"软"约束**：
- 它被注入为普通的用户上下文，模型**概率性地**遵守
- 如果想做到**确定性的**约束（如"绝对不要删除这个文件夹"），必须用权限规则（deny list）
- 这种设计区分了"建议"和"规则"，前者交给 AI 判断，后者由系统强制执行

**记忆按需加载**：
- 不是将所有记忆都注入上下文（会浪费 token）
- 而是先让 LLM 扫描 `MEMORY.md` 索引，选出最多 5 条最相关的
- 选择依据是记忆文件 frontmatter 中的 `description` 字段

**工具列表动态调整**：
- 被权限规则 deny 掉的工具，直接从元数据中移除
- 模型根本"看不到"它不能使用的工具，避免提出无法执行的建议
- MCP 服务器断连时，其工具也会自动移除

---

## 四、第 4 步深入：5 阶段压缩管线

这是整个 Agent 循环中最精妙的部分。上下文窗口有限且昂贵，必须在**信息密度**和 **Token 成本**之间取得平衡。5 个阶段按成本从低到高依次执行：

```
阶段 1: Budget Reduction      ← 最便宜（纯算术截断）
    ↓ 不够
阶段 2: Snip                   ← 裁剪最早历史
    ↓ 不够
阶段 3: Microcompact            ← 缓存感知细粒度压缩
    ↓ 不够
阶段 4: Context Collapse        ← 虚拟投影（非破坏性）
    ↓ 不够
阶段 5: Auto-Compact            ← 最昂贵（模型生成摘要）
```

### 4.1 阶段 1：Budget Reduction（始终活跃）

**作用**：限制单条消息的最大 Token 数。

- 不修改任何历史内容，只在当前轮次对输入做截断
- 成本极低——纯算术操作，不涉及 API 调用
- 主要目的：防止单条 tool result（如读取一个大文件）撑爆上下文

**执行方式**：计算当前上下文总量，如果加上新内容超限，按比例截断新内容中最不重要的部分（如保留文件路径和关键行号，去掉中间的大段代码）。

### 4.2 阶段 2：Snip（功能门控）

**作用**：直接裁剪掉最早的对话历史消息。

```
[消息1] [消息2] [消息3] ... [消息18] [消息19] [消息20]
   ✂️ 删除最早 n 条             ← 保留最近 m 条 →
```

- 只在历史消息数量超过预设阈值时才触发
- 简单粗暴但有效：直接丢弃早期消息，释放窗口空间
- **主要风险**：可能丢失重要上下文（如用户最初提到的需求）
- 所以 Snip 只在其他手段不够时使用，且会尽量保留第一条用户消息

### 4.3 阶段 3：Microcompact（始终活跃，最精妙）

**作用**：缓存感知的细粒度压缩。

这是 Claude Code 针对 **Anthropic API Prompt Caching** 特性专门设计的优化：

**背景知识 — Anthropic Prompt Caching**：
- API 会缓存请求的**前缀部分**（从开头到某个断点）
- 缓存命中时，这些 token 的价格降低 **90%**
- 缓存边界由 API 内部决定，客户端无法精确控制
- 只要前缀不变，缓存就保持命中

**Microcompact 的工作原理**：

```
原始上下文分块结构（每个 ★ 是一个缓存边界）：

┌────────────────────┐ ← 缓存块 A（命中，便宜）
│ 系统提示            │
│ 环境信息            │
│ CLAUDE.md          │
├────────────────────┤ ← 缓存块 B（命中，便宜）
│ 第 1 轮对话        │
│ 第 2 轮对话        │
│ 第 3 轮对话        │
├────────────────────┤ ← 缓存块 C（未命中，全价）
│ 第 4 轮对话        │  ← 新增内容，破坏了后续缓存
│ 第 5 轮对话（当前） │
└────────────────────┘

Microcompact 的策略：
1. 精确保留缓存块 A 和 B 不变（保护缓存命中）
2. 只压缩缓存块 C 中的内容
3. 压缩方式：保留关键信息（文件路径、函数名、错误信息）
   去掉冗余细节（完整代码块、重复描述）
```

**关键洞察**：
- 常规压缩只关注"减少 token 数"
- Microcompact 额外关注"保护缓存块不被破坏"
- 因为缓存命中的 token 便宜 90%，所以减少缓存未命中比减少总 token 更重要
- 这需要精确追踪每个块的缓存状态，计算操作对缓存命中的影响

**代价与收益**：
- 实现复杂度高（需要缓存边界追踪逻辑）
- 但能显著降低 API 成本（大量请求享受 90% 缓存折扣）
- 是 Claude Code 在成本优化上与其他 Agent 框架的关键差异

### 4.4 阶段 4：Context Collapse（非破坏性）

**作用**：读取时的虚拟投影，不修改底层数据。

与 Snip 不同，Context Collapse **不删除**历史消息，而是在模型读入时提供"投影视图"：

```
实际存储的消息（完整保留在内存中）：
[消息1: 完整, 800 tokens]
[消息2: 完整, 1200 tokens]
[消息3: 完整, 600 tokens]
...
[消息20: 完整, 400 tokens]

模型看到的投影：
[消息1-10: 合并摘要, 300 tokens]  ← 10 条合并为 1 条摘要
[消息11: 完整, 350 tokens]
[消息12: 完整, 500 tokens]
...
[消息20: 完整, 400 tokens]
```

**核心优势**：
1. **非破坏性**：完整历史随时可用，投影只是计算出来的视图
2. **缓存友好**：底层数据不变，缓存块不受影响
3. **渐进式**：可以逐步细化摘要（先粗后细）
4. **可回溯**：如果模型需要早期上下文，可以"展开"投影

**实现方式**：
- 维护一个从"投影索引"到"实际消息范围"的映射
- 发送给 API 时使用投影视图
- 当模型引用被折叠的内容时，下一次请求中展开相关部分

### 4.5 阶段 5：Auto-Compact（最后手段）

**作用**：使用模型生成压缩摘要替换历史消息。

这是最昂贵但也是最强大的压缩手段：

```
压缩前（3000 tokens）：
User: 修复登录页面的 bug
Assistant: 我来分析这个问题... [Read: login.tsx]
[Tool Result: login.tsx 内容，200 行]
Assistant: 我发现问题了，在 auth.ts 中... [Read: auth.ts]
[Tool Result: auth.ts 内容，150 行]
Assistant: 问题出在 validateToken 函数...

压缩后（150 tokens）：
[摘要] 用户请求修复登录 bug。助手读取了 login.tsx(200行)
和 auth.ts(150行)。问题定位在 auth.ts 的 validateToken 函数，
该函数在 token 过期时没有正确处理 refresh 逻辑。
关键文件：login.tsx, auth.ts
```

**关键特性**：
- 只在其他 4 个阶段都无法满足预算时才触发
- 每次压缩**消耗一次模型调用**（通常用更便宜的模型做摘要）
- 压缩后的摘要**不可逆**（原始消息被替换，无法恢复）
- 摘要中会保留对工具调用和关键文件路径的引用

**风险**：
- 信息丢失是永久性的
- 如果摘要把关键细节"概括"掉了，后续对话质量会下降
- 这就是为什么它被放在最后——**只在绝对必要时使用**

---

## 五、恢复机制（Resilience）

Agent 循环中可能遇到各种失败，Claude Code 有 5 种恢复策略：

### 5.1 最大输出 Token 升级

```
第 1 次尝试: max_tokens = 4096
    模型输出在 4096 处截断 → 不完整
          ↓
第 2 次尝试: max_tokens = 8192
    模型输出在 8192 处截断 → 仍然不完整
          ↓
第 3 次尝试: max_tokens = 16384
    模型输出完整 → 成功
          ↓
最多 3 次重试, 每次将 max_tokens 翻倍
```

### 5.2 响应式压缩

```
Tool Result 太大, 无法放入上下文：
          ↓
计算需要压缩的比例 (如需要缩减 40%)
          ↓
对 tool result 做智能截断:
  · 保留文件路径、行号、函数名、错误信息
  · 截断大段的完整代码、重复的日志输出
  · 优先保留"靠近光标"的内容（与用户当前关注点相关）
          ↓
重新组装上下文 → 继续循环
```

注意：这和 5 阶段压缩不同。响应式压缩是在**单条 tool result** 层面的应急处理。

### 5.3 Prompt-Too-Long 处理

```
API 返回 "prompt too long" 错误：
          ↓
触发 Context Collapse（虚拟投影）
          ↓
仍然超限 → 触发 Auto-Compact（模型摘要）
          ↓
仍然超限 → 触发 Snip（裁剪最早历史）
          ↓
仍然超限 → 报告错误给用户，建议开启新会话
```

这是一个级联回退的过程，从非破坏性到破坏性逐级升级。

### 5.4 流式回退

```
流式请求失败 (stream: true 出错):
          ↓
重试非流式请求 (stream: false)
        非流式更稳定但用户体验差（无实时输出）
          ↓
如果非流式也失败 → 进入模型切换
```

### 5.5 模型切换

```
主模型连续失败 (如 Opus):
          ↓
自动切换到备用模型 (如 Sonnet)
          ↓
用备用模型完成当前回合
          ↓
下一回合尝试切回主模型
如果主模型仍然失败 → 继续使用备用模型
```

---

## 六、一个完整回合的时间线示例

以用户说"修复登录页面的 bug"为例：

```
t = 0ms      用户输入："修复登录页面的 bug"

t = 1ms      [步骤 1] 加载 settings.json，解析配置
              · API Base URL
              · 模型映射
              · 超时设置

t = 2ms      [步骤 2] 状态初始化
              · 追加用户消息到会话 JSONL
              · 检查并加载 MEMORY.md

t = 3ms      [步骤 3] 上下文组装：
              · 系统提示（固定模板，约 2000 tokens）
              · 环境：win32, bash, E:\AI_TEST
              · CLAUDE.md（4 级合并，约 500 tokens）
              · 记忆：选出了 2 条相关记忆（约 150 tokens）
              · 工具列表：当前 18 种可用工具的定义（约 3000 tokens）
              · 对话历史：上次会话的压缩摘要（约 200 tokens）
              · 本轮用户消息

t = 50ms     [步骤 4] 预模型整形：
              · Budget: 当前总 token 约 8000，预算上限 120000，无需削减
              · Microcompact: 缓存命中 3 个块，无需压缩
              · 其他阶段跳过

t = 51ms     [步骤 5] 开始 POST 请求到 API

t = 2000ms   [步骤 5] 流式响应开始返回第一个 token

t = 3500ms   [步骤 5] 模型输出完成：
              文本: "我先来看看登录页面的代码。"
              工具调用: Read(file_path="src/pages/login.tsx")

t = 3501ms   [步骤 6] 解析出 1 个 tool_use 块 → Read 工具

t = 3502ms   [步骤 7] 权限检查：
              · 预过滤：Read 工具未被过滤 ✓
              · Hook：无 PreToolUse hook 阻止 ✓
              · 规则：Read("src/pages/login.tsx") 匹配 allow 规则 Read(*) ✓
              · 权限模式 default，需要审批

t = 4000ms   [步骤 7] 用户点击"批准"

t = 4001ms   [步骤 8] 执行 Read 工具
              · 读取 src/pages/login.tsx
              · 成功，返回 120 行代码

t = 4010ms   [步骤 9] 停止条件检查：
              · 有工具调用结果，模型需要继续处理
              · 轮次计数：1 / 最大 50
              · 用户未中断
              · → 回到步骤 3

--- 第二轮 ---

t = 4011ms   [步骤 3] 上下文组装：
              · 新增：上轮的工具调用记录 + Read 结果（约 1500 tokens）
              · 总 token：约 9500

t = 4050ms   [步骤 4] 预模型整形：
              · 仍然在预算内，无需压缩

t = 4051ms   [步骤 5] 发送到 API...

...循环继续直到模型不再输出 tool_use，而是直接给用户回复...
```

---

## 七、关键设计决策深度分析

### 7.1 所有接口共享同一个 queryLoop

| 接口 | 差异处理方式 |
|------|------------|
| **CLI (交互式)** | 使用 React+Ink UI，权限审批通过界面交互 |
| **Headless (`-p`)** | 跳过交互式审批，使用 auto 或 bypassPermissions |
| **SDK** | 提供编程式回调替代界面审批 |
| **IDE 扩展** | 在 IDE 面板中渲染审批 UI |

这些差异通过**包装器**（wrapper）处理，而不是维护多个循环实现。包装器负责：
- 如何收集权限审批结果
- 如何展示流式输出
- 如何处理用户中断

核心的 `queryLoop` 函数对这一切无感知——它只接收"是否批准"的布尔值。

### 7.2 压缩可逆性梯度

```
完全可逆 ←──────────────────────────────→ 完全不可逆

Budget Reduction    Microcompact   Context Collapse    Snip    Auto-Compact
(截断可恢复)        (等义变换)     (投影可展开)      (消息删除) (摘要替换)
```

**设计原则**：尽可能偏向左侧，只在绝对必要时才向右移动。这个梯度的存在意味着：
- 前 3 个阶段可以"大胆"使用（代价低、可逆）
- Snip 和 Auto-Compact 要"谨慎"使用（代价高、不可逆）
- 每回合最多做一次不可逆压缩（防止压缩级联）

### 7.3 缓存感知压缩（Microcompact）的工程价值

这是 Claude Code 与一般 Agent 框架最独特的差异点：

| 对比维度 | 一般框架 | Claude Code |
|---------|---------|-------------|
| 缓存意识 | 无 | 精确追踪缓存边界 |
| 压缩策略 | 只看 token 数 | token + 缓存命中率 |
| 成本优化 | 粗粒度 | 细粒度（块级别） |
| API 成本 | 较高 | 缓存命中时降低 90% |

实现这一功能需要：
1. 对 Anthropic API 缓存机制的深入理解
2. 精确追踪每轮对话后缓存边界的变化
3. 压缩算法中注入缓存命中率计算
4. 在"信息密度"和"缓存成本"之间做动态决策

### 7.4 每回合只压缩一次

这是为了打破"压缩级联"：

```
压缩 → 信息丢失 → 模型输出差 → 需要更多轮次
  → 上下文更快填满 → 再次压缩 → 更多信息丢失
  → 模型输出更差 → ...
```

限制每回合一次压缩，给模型足够的时间在压缩前的完整上下文中工作，避免了这种恶性循环。

### 7.5 轮次上限

Agent 循环有最大轮次限制（通常 50 轮）。达到上限后自动停止。这防止了：
- 模型陷入死循环（反复调用同一工具）
- 代理失控（无限调用工具消耗 API 预算）
- 上下文无限膨胀

---

## 八、与标准 ReAct 模式的对比

| 维度 | 标准 ReAct | Claude Code |
|------|-----------|-------------|
| 循环结构 | Thought → Action → Observation | 9 步管道，含压缩和权限 |
| 工具调用 | 单个工具 | 可并行调用多个工具 |
| 上下文管理 | 简单追加 | 5 级压缩管线 |
| 权限 | 无内置 | 7 种模式 + 4 级授权 |
| 恢复机制 | 无 | 5 种恢复策略 |
| 持久化 | 通常无 | JSONL 转录 + Sidechain |
| 缓存感知 | 无 | Microcompact 缓存优化 |

Claude Code 的 Agent 循环本质上是一个**工业级的 ReAct 实现**，核心循环结构相同，但在上下文管理、安全模型、可靠性保障上做了大量的工程化工作。

---

> 基于调研结果整理，更新时间：2026-05-29
